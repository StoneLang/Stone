
1. Future structure: Core, Analyze(Lexer, Parser, Checker), Gen, Compile, Driver, Session

2.
compile(0): {test.stone} -> object
compile(1): {sample.stone} -> object 
link: {compile(0),compile(1)} -> image 

3. Implement LinkType 
