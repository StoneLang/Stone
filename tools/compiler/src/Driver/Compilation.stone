

space Stone.Driver {

	public struct Compilaiton  {	
		private Driver* driver = null; 
	}

	// Compilation is public so Init is public by default?
	init fun Compilation::Init(Driver* driver) {
		self.driver = driver; 
	}
	defer fun Compilation::Defer() {
	}
}

