
use STD.IO; 
use STD.Format; 

use Stone.Frontend; 
use Stone.Compilation; 


fun Main(string[] args) -> i32  {

 	if (args.Size() == 0){
   		return -1; 
	}
  for (int i = 0; i < args.Size(); i++) {
    	Print(args[i]);
  }
    return 0; 
}
