
use System.IO; 
use Stone.Format; 

fun Main(string[] args) -> i32  {

 	if (args.Size() == 0){
   		return -1; 
	}
  for (int i = 0; i < args.Size(); i++) {
    	Print(args[i]);
  }
    return 0; 
}
